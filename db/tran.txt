транзакции
- группа операций над базой, 
каждая которая либо целиком выполнится, 
либо в конце зафейлится  
- группа выполняющих команд, 
которые можно подтвердить/отменить целиком
Транзакция
объединяет последовательность
действий в одну операцию «всё или
ничего».
Реализация транзакций в СУБД
PostgreSQL основана на
мультиверсионной модели (Multiversion
Concurrency Control, MVCC).


Характеристики транзакций

В конце 1970-х Джим Грей выделил следующие характеристики транзакций:

    Атомарность (Atomicity)
    Непротиворечивость (Consistency)
    Изолированность (Isolation) 
    Долговечность (Durability)

Атомарность означает, что все действия, производимые в рамках одной транзакции атомарны. 
Если транзакция заканчивается не успешно, то все изменения, сделанный в рамках этой транзакции откатываются назад. 
Если транзакция заканчивается успешно, то все её изменения сохраняются.

Непротиворечивость означает, что транзакция переводит базу данных из одного непротиворечивого состояния в другое непротиворечивое состояние. 
При этом, внутри транзакции база данных может находиться в противоречивом состоянии.

Изолированность означает, что во время работы одной транзакции ей недоступны (с поправками на уровень изоляции) 
изменения, происходящие в другой транзакции.

Под долговечностью подразумевают, что после окончания транзакции все изменения, сделанные транзакцией фиксируется навсегда,
то есть, если происходит программно-аппаратный сбой, то после восстановления системы в рабочее состояние нет потерянных изменений.


При параллельном выполнении нескольких транзакций могут возникнуть следующие проблемы:

    Потерянное обновление
    “Грязное” чтение
    Неповторяющееся чтение
    Фантомная вставка

Потерянное обновление может возникнуть в ситуации, когда две транзакции обновляют одни и те же данные.

“Грязное” чтение возникает, когда одна транзакция видит несохранённые изменения, сделанные другой транзакцией.

Неповторяющееся чтение - это ситуация, когда в рамках одной транзакции один и тот же запрос на выборку возвращает разные результаты.

Фантомная вставка - ситуация, похожая на неповторяющее чтение - 
заключается в том, что результаты одной и той же выборки в рамках одной транзакции имеют разное количество возвращаемых строк.

Стандарт SQL определяет следующие уровни изоляции транзакции:

    READ_UNCOMMITED
    READ_COMMITED
    REPEATABLE_READ
    SERIALIZABLE

READ_UNCOMMITED - позволяет другим транзакциям видеть несохранённые изменения

READ_COMMITED — транзакция во время выполенения видит только зафиксированные изменения. 
В большинстве баз данных это уровень изоляции по умолчанию.

REPEATABLE_READ — данный уровень изоляции решает проблему неповторяющего чтения.
(следит, чтоб не меняли одинаковые строчки)

SERIALIZABLE - данный уровень изоляции выполняет все транзакции последовательно. 
Это самый надёжный уровень изоляции, но, при этом, и наименее производительный.
при параллельном выполнении несколько сериализуемых транзакций должны гарантированно выдавать такой же результат, 
как если бы они запускались по очереди в некотором порядке. 

В Postgres Pro вы можете запросить любой из четырёх уровней изоляции транзакций, однако внутри реализованы только три различных уровня, 
то есть режим Read Uncommitted в Postgres Pro действует как Read Committed.


MVCC - одна из ключевых технологий доступа к данным, которая используется в PostgreSQL. 
Она позволяет осуществлять параллельное чтение и изменение записей (tuples) одних и тех же таблиц без блокировки этих таблиц. 
Чтобы иметь такую возможность, данные из таблицы сразу не удаляются, а лишь помечаются как удаленные.






В PostgreSQL для просмотра плана запроса
 можно использовать команду EXPLAIN. 
 Эта команда позволяет получить информацию о том,
  как PostgreSQL будет выполнять запрос, включая информацию о том, какие индексы будут использованы, сколько строк будет обработано и т.д.

Пример использования:

sql

EXPLAIN SELECT * FROM your_table WHERE your_column = 'some_value';

Если вам нужно более подробное объяснение, 
вы можете использовать EXPLAIN ANALYZE, 
который выполнит запрос и предоставит
 фактические данные о его выполнении:

sql

EXPLAIN ANALYZE SELECT * FROM your_table WHERE your_column = 'some_value';

Row Level и Advisory Locks

Row Level Locks (Блокировки на уровне строк) — 
это блокировки, которые применяются к 
отдельным строкам таблицы. Они используются, 
когда нужно предотвратить одновременное 
изменение одной и той же строки несколькими 
транзакциями. 
Например, если одна транзакция обновляет строку,
 другие транзакции, пытающиеся изменить эту же строку, будут заблокированы до завершения первой транзакции.

Advisory Locks (Консультативные блокировки) — 
это механизмы блокировок, 
которые позволяют пользователям или приложениям
 устанавливать свои собственные блокировки на 
 произвольные ресурсы. 
 Это полезно, когда необходимо синхронизировать доступ к неявным ресурсам, которые не являются строками таблицы. Advisory locks не блокируют стандартные операции и могут использоваться для реализации более сложных схем синхронизации.

Для работы с консультативными блокировками используются функции pg_advisory_lock() и pg_advisory_unlock(). Например:

sql

SELECT pg_advisory_lock(1); -- Установить блокировку
SELECT pg_advisory_unlock(1); -- Освободить блокировку

Таким образом, каждый из типов блокировок решает свои задачи и используется в различных сценариях.