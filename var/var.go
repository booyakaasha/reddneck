package main

import "fmt"

func main() {
	// значение по умолчанию
	var num0 int

	// значение при инициализации
	var num1 int = 1

	// пропуск типа
	var num2 = 20
	fmt.Println(num0, num1, num2)

	// короткое объявление переменной
	num := 30
	// только для новых переменных
	// no new variables on left side of :=
	// num := 31

	num += 1
	fmt.Println("+=", num)

	// ++num нет
	num++
	fmt.Println("++", num)

	// camelCase - принятый стиль
	userIndex := 10
	// under_score - не принято
	user_index := 10
	fmt.Println(userIndex, user_index)

	// объявление нескольких переменных
	var weight, height int = 10, 20

	// присваивание в существующие переменные
	weight, height = 11, 21

	// int - платформозависимый тип, 32/64
	var i int = 10

	// автоматически выбранный int
	var autoInt = -10

	// int8, int16, int32, int64
	var bigInt int64 = 1<<32 - 1

	// платформозависимый тип, 32/64
	var unsignedInt uint = 100500

	// uint8, unit16, uint32, unit64
	var unsignedBigInt uint64 = 1<<64 - 1

	fmt.Println(i, autoInt, bigInt, unsignedInt, unsignedBigInt)

	// float32, float64
	var pi float32 = 3.141
	var e = 2.718
	goldenRatio := 1.618

	fmt.Println(pi, e, goldenRatio)

	// bool
	var b bool // false по-умолчанию
	var isOk bool = true
	var success = true
	cond := true

	fmt.Println(b, isOk, success, cond)

	// complex64, complex128
	var c complex128 = -1.1 + 7.12i
	c2 := -1.1 + 7.12i

	fmt.Println(c, c2)

	// короткое присваивание
	// хотя-бы одна переменная должна быть новой!
	weight, age := 12, 22

	fmt.Println(weight, height, age)
}

/*
▎Основные вопросы

1. Что такое переменные в Go?
   - Переменные в Go — это именованные области памяти,
   которые могут хранить значения.
   Они позволяют программе работать с данными и изменять их в процессе выполнения.


3. Что такое типы данных в Go?
   - Go поддерживает статическую типизацию
   и имеет несколько основных типов данных,
   таких как int, float64, string, bool, а также более сложные структуры, массивы и срезы.

4. Какова разница между статической и динамической типизацией?
   - В статической типизации (как в Go) тип переменной определяется во время компиляции,
   что позволяет обнаруживать ошибки на этапе компиляции.
   В динамической типизации тип переменной определяется во время выполнения.

5. Что такое нулевая переменная?
   - Нулевое значение — это значение, которое имеет переменная до того, как ей было присвоено какое-либо другое значение.
   Например, для int это 0, для string — пустая строка, для указателей — nil.


6. Как работают константы в Go?
   - Константы объявляются с помощью ключевого слова const
   и не могут быть изменены после объявления:

     const Pi = 3.14


7. Что такое срезы и как они связаны с переменными?
   - Срезы — это динамические массивы, которые могут изменять свой размер.
   Они являются ссылочными типами, что означает, что при передаче среза в функцию передается ссылка на данные.

8. Какова разница между указателями и значениями?
   - Указатели содержат адреса переменных,
   а не сами значения.
   Это позволяет изменять значение переменной внутри функции,
   передавая указатель на неё.

9. Каковы правила видимости переменных в Go?
   - Переменные, начинающиеся с заглавной буквы, доступны за пределами пакета (публичные),
   а начинающиеся со строчной — только внутри пакета (приватные).

10. Что такое анонимные переменные?
    - Анонимные переменные обозначаются символом _ и используются для игнорирования значений, которые не нужны.


11. Напишите функцию, которая принимает переменные разных типов.

    func PrintValue(v interface{}) {
        fmt.Println(v)
    }


12. Объясните, как можно использовать defer с переменными.
    - defer откладывает выполнение функции до завершения окружающей функции:

    func example() {
        x := 10
        defer fmt.Println(x) // Выводит 10 после завершения функции
    }


13. Как вы можете изменить значение переменной внутри функции?
    - Для изменения значения переменной внутри функции можно передать указатель:

    func changeValue(val *int) {
        *val = 20
    }


14. Как работают глобальные переменные в Go?
    - Глобальные переменные объявляются вне функций и доступны во всех функциях пакета.
	 Однако использование глобальных переменных может привести к проблемам с состоянием и тестированием.

15. Приведите пример использования структуры и полей в ней как переменных.

    type Person struct {
        Name string
        Age  int
    }

    func main() {
        p := Person{Name: "Alice", Age: 30}
        fmt.Println(p.Name)
    }

Внутри
функций вместо объявления переменных с помощью ключевого слова var мож-
но использовать оператор :=, который производит автоматический вывод типа
переменной.

В то же время оператор := способен на один трюк, который недоступен при ис-
пользовании ключевого слова var: с его помощью можно присвоить значение
существующей переменной. Если слева от оператора := будет указана хотя бы
одна новая переменная, то любая из остальных переменных может представлять
собой уже существующую переменную:
x := 10
x, y := 30, "hello"

Оператор := обладает одним ограничением: при объявлении переменной на
уровне пакета необходимо использовать ключевое слово var, поскольку опера-
тор := нельзя использовать вне функций.

func main() {
x := 10
if x > 5 {
fmt.Println(x)
x := 5
fmt.Println(x)
}
fmt.Println(x)
}

На самом деле этот код выведет следующее:
10
5
10
Переменная является затеняющей, если ее имя совпадает с именем переменной,
определенной во вмещающем блоке. При наличии затеняющей переменной вы
не можете получить доступ к затененной переменной.

значение nil языка Go представляет собой нетипизиро-
ванный идентификатор, представляющий случай отсутствия значения для опре-
деленных типов. В отличие от значения NULL языка C, значение nil не является
просто псевдонимом для значения 0: вы не можете преобразовать значение nil
в число и, наоборот, преобразовать число в значение nil.

Поскольку Go — язык с передачей параметров по значению, передаваемые
функциям значения представляют собой копии. В случае таких неуказательных
типов, как простые типы, структуры и массивы, это означает, что вызываемая
функция не может изменить оригинал. Вызываемая функция получает копию
исходных данных, что гарантирует неизменность исходных данных.



*/
