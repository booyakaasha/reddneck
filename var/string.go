package main

import (
	"fmt"
	"unicode/utf8"
)


	// пустая строка по-умолчанию
	var str string

	// со спец символами
	var hello string = "Привет\n\t"

	// без спец символов
	var world string = `Мир\n\t`

	fmt.Println("str", str)
	fmt.Println("hello", hello)
	fmt.Println("world", world)

	// UTF-8 из коробки
	var helloWorld = "Привет, Мир!"
	hi := "你好，世界"

	fmt.Println("helloWorld", helloWorld)
	fmt.Println("hi", hi)

	// одинарные кавычки для байт (uint8)
	var rawBinary byte = '\x27'

	// rune (uint32) для UTF-8 символов
	var someChinese rune = '茶'

	fmt.Println(rawBinary, someChinese)

	helloWorld = "Привет Мир"
	// конкатенация строк
	andGoodMorning := helloWorld + " и доброе утро!"

	fmt.Println(helloWorld, andGoodMorning)

	// строки неизменяемы
	// cannot assign to helloWorld[0]
	// helloWorld[0] = 72

	// получение длины строки
	byteLen := len(helloWorld)                    // 19 байт
	symbols := utf8.RuneCountInString(helloWorld) // 10 рун

	fmt.Println(byteLen, symbols)

	// получение подстроки, в байтах, не символах!
	hello = helloWorld[:12] // Привет, 0-11 байты
	H := helloWorld[0]      // byte, 72, не "П"
	fmt.Println(H)

	// конвертация в слайс байт и обратно
	byteString := []byte(helloWorld)
	helloWorld = string(byteString)

	fmt.Println(byteString, helloWorld)
}

/*
▎Основные аспекты строк в Go

1. Тип строки:
   - В Go строки представляют собой 
   последовательности байтов и имеют тип string.
    Они неизменяемы, то есть 
    после создания строки её содержимое 
    нельзя изменить.

2. Создание строк:
   - Строки можно создавать с помощью литералов:
     
     str1 := "Hello, World!"
     str2 := Это строка в обратных кавычках. // Многострочные строки
     

3. Длина строки:
   - Длину строки можно узнать 
   с помощью функции len():
     
     length := len(str1) // возвращает количество байтов
     

4. Индексация строк:
   - Строки можно индексировать, 
   чтобы получить отдельные байты:
     
     firstByte := str1[0] // 'H'
     

5. Конкатенация строк:
   - Строки можно объединять 
   с помощью оператора +:
     
     combined := str1 + " " + str2
     

6. Сравнение строк:
   - Строки можно сравнивать 
   с помощью операторов == и !=:
     
     if str1 == str2 {
         // ...
     }
     

7. Форматирование строк:
   - Для форматирования строк можно
    использовать пакет fmt:
     
     formatted := fmt.Sprintf("Имя: %s, Возраст: %d", "Alice", 30)
     

8. Преобразование строк:
   - Строки можно преобразовывать в 
   другие типы, например, в []byte или []rune:
     
     byteArray := []byte(str1)
      // преобразует строку в массив байтов
     runeArray := []rune(str1) 
     // преобразует строку в массив рун (символов)
     

9. Разделение строк:
   - Для разделения
    строки на подстроки можно использовать
     функцию strings.Split() из пакета strings:
     
     import "strings"
     
     parts := strings.Split("a,b,c", ",") // возвращает []string{"a", "b", "c"}
     

10. Поиск подстроки:
    - Для поиска подстроки можно использовать 
    strings.Contains() или strings.Index():
      
      contains := strings.Contains(str1, "Hello") // возвращает true
      index := strings.Index(str1, "World")        // возвращает индекс начала подстроки
      

Строки в go можно складывать(конкатенировать). 
Для многих операций есть стандартные пакеты, к примеру strings, fmt. Кроме того, надо понимать, что все варианты конкатенации имеют свою производительность. 
"Что будет если сложить строки?"
Ранее мы говорили о том что, строки - это массивы байт. Из этого следует, что при работе со строками (конкатенация и тд)
 мы будем получать новые строки.

"Как определить количество символов для строки?" 
или "Какие есть нюансы при итерации по строке?"

Исходя из того же знания, что строка это массив 
байт, взяв базовую функцию len() от строки мы получим количество байт. 
Похожее поведение будет при итерации 
по строке - итерация по байтам. 
Тогда как в зависимости от кодировки, 
символ в строке может занимать не один байт.
Для того, чтобы работать именно с символами,
 необходимо преобразовать строку в тип []rune.
Еще одним способом определения длинны 
строки является функция RuneCountInString пакета utf8.
func main() {
        str := "世界, 你好!"
        fmt.Printf("len bytes: %d\n", len(str))
        fmt.Printf("len runes: %d\n", len([]rune(str)))
  fmt.Printf("len runes: %d\n", utf8.RuneCountInString(str))
}

"Как преобразовать строку в int и наоборот? 
Можно ли сделать int(string) и string(int) соответственно?"
Преобразование типов между int и string 
указанным синтаксисом невозможно. 
Для преобразования необходимо использовать
 функции из пакета strconv стандартной
  библиотеки go.
При этом для преобразования 
строк в/из int и int64 используются разные функции, 
strconv.Atoi и strconv.Itoa для int,
 strconv.ParseInt и strconv.FormatInt соответственно.



▎Пример работы со строками

Вот пример программы, которая демонстрирует некоторые из вышеперечисленных операций со строками:

package main

import (
    "fmt"
    "strings"
)

func main() {
    str1 := "Hello, World!"
    str2 := "Go is awesome!"

    // Конкатенация
    combined := str1 + " " + str2
    fmt.Println(combined)

    // Длина строки
    fmt.Println("Length of str1:", len(str1))

    // Индексация
    fmt.Println("First character of str1:", string(str1[0]))

    // Разделение строки
    parts := strings.Split(combined, " ")
    fmt.Println("Parts:", parts)

    // Поиск подстроки
    if strings.Contains(combined, "Go") {
        fmt.Println("Found 'Go' in combined string.")
    }
}
    
. Что такое тип rune? Зачем их использовать?

rune — это встроенный тип данных в Go, 
который представляет 
собой 32-битный целочисленный тип (аналог int32), и используется для хранения Unicode кода символа.

Go использует тип rune, 
чтобы более точно работать с 
символами в строках, так как строки в Go — это последовательности байтов (тип byte), а каждый символ Unicode может занимать более одного байта.
Пример:

go

var r rune = 'A'
fmt.Println(r)  // Вывод: 65 
(код символа 'A' в Unicode)

Использование rune необходимо, 
когда вам нужно обрабатывать символы Unicode или выполнять операции с символами, выходящими за пределы однобайтовых значений.
5. Что такое тип byte?

byte — это синоним типа uint8, 
который представляет собой 8-битное
 беззнаковое целое число. Этот тип часто используется для работы с массивами байтов или данными, которые нужно представить в байтах.
Пример:

go

var b byte = 255
fmt.Println(b)  // Вывод: 255

Тип byte часто используется для 
работы со строками и массивами байтов,
 так как строки в Go представляют собой
  последовательности байтов ([]byte).

*/