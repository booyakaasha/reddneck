package main

/*

if err != nil {
fmt.Println(err)

Тип error представляет собой встроенный интерфейс, который определяет
метод:
type error interface {
Error() string
}

Есть две веские причины, по которым в Go принято возвращать ошибки, а не
выбрасывать исключения. Во-первых, исключения добавляют как минимум
один дополнительный путь выполнения кода. Эти пути выполнения кода ино-
гда бывают неясными, особенно в тех языках, в которых функции не содержат
объявление о возможности исключения. В результате вы можете получить код,
который будет неожиданно прекращать работу в случае неправильной обработки
исключений или, что еще хуже, будет продолжать работу после некорректного
выполнения инициализации, модификации или сохранения данных.
Вторая причина носит не столь явный характер, но показывает, насколько хоро-
шо различные элементы языка Go согласуются друг с другом. Компилятор языка
Go требует, чтобы все объявленные переменные были прочитаны. В силу того
что ошибки являются возвращаемыми значениями, разработчикам приходится
либо проверять наличие состояний ошибки и обрабатывать их, либо явно ука-
зывать, что ошибки будут игнорироваться, используя в качестве возвращаемого
значения ошибки символ подчеркивания (_).


Первый способ сводится к использованию функции errors.New, ко-
торая принимает параметр типа string и возвращает значение типа error.

func doubleEven(i int) (int, error) {
if i % 2 != 0 {
return 0, errors.New("only even numbers are processed")
}
return i * 2, nil
}


Второй способ создания ошибки сводится к использованию функции fmt.
Errorf, которая позволяет задействовать все глаголы форматирования функ-
ции fmt.Printf.

После выдачи паники немедленно
прекращается выполнение текущей функции и запускаются все отложенные
функции, привязанные к текущей функции. После выполнения этих отло­
женных функций запускаются отложенные функции, привязанные к вызы­
вающей функции, и так далее, пока не будет достигнута функция main. После
этого производится выход из программы с выводом сообщения и трассировкой
стека.

Go предоставляет возможность перехватить панику, чтобы обеспечить более
мягкое прекращение работы или вообще обойтись без него.
!!!!!!!!!!Для этого встро-
енная функция recover вызывается внутри оператора defer, чтобы проверить,
была ли паника. При наличии паники возвращается присвоенное ей значение.
После выполнения функции recover работа продолжается в обычной манере.

func div60(i int) {
defer func() {
if v := recover(); v != nil {
fmt.Println(v)
}
}()
fmt.Println(60 / i)
}
func main() {
for _, val := range []int{1, 2, 0, 6} {
div60(val)
}
}

Полагаться на функции panic и recover не стоит по той причине, что функ-
ция recover не дает нам информации о том, что могло дать сбой. Она лишь
­гарантирует, что в случае сбоя мы сможем вывести сообщение и продолжить
работу.

Обработка паники в Go — важный аспект, который позволяет избежать аварийного завершения программы и управлять ошибками более эффективно. В Go для этого используется механизм recover, который позволяет "поймать" панику и продолжить выполнение программы. Вот подробное объяснение:

▎1. Что такое паника в Go?

Паника (panic) в Go — это механизм, который используется для сигнализации о фатальных ошибках.
Когда происходит паника, программа немедленно прекращает выполнение текущей функции и начинает "откатываться" обратно по стеку вызовов, пока не найдет функцию, которая обработает панику с помощью recover.

▎2. Как вызвать панику?

Паника может быть вызвана с помощью функции panic():

panic("что-то пошло не так")


▎3. Как обработать панику?

Чтобы обработать панику, необходимо использовать defer и recover.
Функция, в которой вы хотите обработать панику, должна быть отложена с помощью defer.
Внутри этой функции вы можете вызвать recover, чтобы поймать панику.

Вот пример:

package main

import (
    "fmt"
)

func riskyFunction() {
    defer func() {
        if r := recover(); r != nil {
            fmt.Println("Паника поймана:", r)
        }
    }()

    // Здесь может произойти паника
    panic("что-то пошло не так")
}

func main() {
    fmt.Println("Начало программы")
    riskyFunction()
    fmt.Println("Конец программы")
}


▎4. Как это работает?

1. defer: Отложенная функция выполняется в момент выхода из окружающей функции,
даже если произошла паника.

2. recover(): Эта функция возвращает значение, переданное в panic(),
если она была вызвана в контексте паники. Если паники нет, recover() вернет nil.

▎5. Когда использовать панику и recover?

• Используйте паники: когда вы сталкиваетесь с ситуациями, которые не должны происходить
(например, выход за пределы массива или серьезные ошибки, которые не могут быть обработаны).

• Используйте recover: для восстановления программы после паники и предотвращения ее аварийного завершения.

▎6. Пример с несколькими уровнями

Вы также можете обрабатывать панику на нескольких уровнях:

package main

import (
    "fmt"
)

func levelOne() {
    defer func() {
        if r := recover(); r != nil {
            fmt.Println("Уровень 1: Паника поймана", r)
        }
    }()
    levelTwo()
}

func levelTwo() {
    defer func() {
        if r := recover(); r != nil {
            fmt.Println("Уровень 2: Паника поймана", r)
        }
    }()

    panic("что-то пошло не так на уровне 2")
}

func main() {
    fmt.Println("Начало программы")
    levelOne()
    fmt.Println("Конец программы")
}


*/
